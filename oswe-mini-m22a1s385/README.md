# v2 Greenfield Routing Replacement

This directory contains a greenfield implementation scaffold for the legacy `issue_project` and integration tests.

Run the integration tests (PowerShell):

    .\setup.sh; .\run_tests.sh

Or run on Unix-like environments:

    ./setup.sh && ./run_tests.sh

See `compare_report.md` for migration guidance and acceptance criteria.
